; Removed pnpm-only settings to avoid warnings when Vercel uses npm.
; If you want to use pnpm on Vercel, set the project's Install Command to `pnpm install`
; and ensure `pnpm-lock.yaml` is committed.

